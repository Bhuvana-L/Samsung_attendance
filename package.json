{
  "name": "samsung-lab-attendance",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "render-build": "cd backend && npm install && cd ../frontend && npm install && CI=false npm run build"
  }
}
